// Q186. Bubble Sort (Ascending)
// Sort an array using bubble sort algorithm.

// Input: [3, 1, 4, 2] → Output: [1, 2, 3, 4]
// Input: [5, 5, 5] → Output: [5, 5, 5]
// Input: [-1, 3, 0] → Output: [-1, 0, 3]

function sort(arr){
    for(let i = 0; i<arr.length; i++){
        for(let j = 1; j<i; j++){
            if(arr[i]>)
        }
    }
}